fastapi[standard]
uvicorn[standard]
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-multipart
python-dotenv
supabase
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
argon2-cffi>=23.1.0
pyjwt>=2.8.0
httpx>=0.27.0
jinja2>=3.1.4
# Dev/test dependencies (included here for simplicity; move to pyproject.toml [dev] for production)
pytest>=8.0.0
pytest-asyncio>=0.23.0
# Note: alembic is NOT listed here — it is a migration tool, not a runtime dependency.
# Run migrations locally or in CI before deploying. See pyproject.toml [dev] deps.